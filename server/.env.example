PORT=4000
DATABASE_URL="postgresql://postgres:password@localhost:5432/2go2?schema=public"
CLIENT_ORIGIN="http://localhost:3000"

# Production note:
# If this app runs on serverless Postgres, use a PgBouncer-safe URL:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?pgbouncer=true&connection_limit=1"
# Keep a separate direct URL for migrations if your provider gives you one.
